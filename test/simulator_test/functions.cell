mod m = {
	def nothing() = {}
	def zero() -> int = 0
	def test(a : int, b : int) -> bool = { a == b }
	def add(a : int, b : int) -> int = { a + b }
	def cond(z : bool, a : int, b : int) -> int = if( z ) a else b

	// def zero() -> int = {
	// 	return 0
	// }


	// def add(a :int, b : int) -> int = {
	// 	return 0 
	// }

}
