namespace demo = {

    socket binop = {
        <= a : int
        <= b : int
        => y : int
    }


    mod adder : binop = {
        var test : int

        process = { port.y = port.a + port.b; }

        process = {
            test = port.a;
        }
    }

    /*mod multiplier : binop = {*/
        /*process = { port.y = port.a * port.b; }*/
    /*}*/


    mod dataflow = {
        var to_plus : binop
        var add_res : int

        def __init__() = {
        }

        process = {
            plus.a = 1;
            plus.b = 2;
            add_res = plus.y;
        }

        adder plus( a : aa, b : bb, y : yy)
        /*adder plus(to_plus)*/
        /*multiplier mul( a : p.y, b : 2, y : mul_res)*/
    }



}
