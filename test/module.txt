mod test_module = {
	var a = 5 : int

	def f(a : int,
				b : int,
				c : int,) : int = {
		var b = 10 + a : int
		var x
		var y : float
		var z = 89 
	}

	def init() = {
		if( 1 ) {
			f(1, 2, 3)
		}

    if( test_func(x) ) {
      do_something()
    } else {
      do_something_else()
    }

    while( i < 5 ) {
      print("something")
    }
	}

	def test() = f(1, 5, 6)
}
